syntax = "proto3";

package ca.tomrobinson.proto; // Requried to generate valid code.

message Address {
    string street = 1;
    string city = 2;
}

message Person {
    string name = 1;
    int32 id = 2;
    string email = 3;
    Address address = 4;
}